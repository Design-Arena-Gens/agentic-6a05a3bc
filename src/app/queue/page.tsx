import { ComingSoon } from "@/components/coming-soon";

export default function QueuePage() {
  return (
    <ComingSoon
      title="Queue control, evolved"
      description="Reorder, crossfade, and build seamless set lists on the fly. Multi-device queue syncing is still in development."
      highlight="Queue"
    />
  );
}
